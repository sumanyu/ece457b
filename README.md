# ece457b

Don't read me

Run `naive_bayes.py` to get baseline.
Run `run.py` to run autoencoder.

Autoencoder currently isn't currently hooked up to the last step of getting hidden representation, feeding it to naive bayes and getting classification results.
